<?php
    // 1번 문제
    /*$number1 = $_POST['number1'];
    $number2 = $_POST['number2'];
    $number3 = $_POST['number3'];
    $number4 = $_POST['number4'];
    $number5 = $_POST['number5'];

    // 짝수, 홀수별 합계 계산
    $evenSum = 0;
    $oddSum = 0;

    if ($number1 % 2 == 0) {
        $evenSum += $number1;
    } else {
        $oddSum += $number1;
    }

    if ($number2 % 2 == 0) {
        $evenSum += $number2;
    } else {
        $oddSum += $number2;
    }

    if ($number3 % 2 == 0) {
        $evenSum += $number3;
    } else {
        $oddSum += $number3;
    }

    if ($number4 % 2 == 0) {
        $evenSum += $number4;
    } else {
        $oddSum += $number4;
    }

    if ($number5 % 2 == 0) {
        $evenSum += $number5;
    } else {
        $oddSum += $number5;
    }

    // 결과 출력
    echo "<p>짝수의 합계: " . $evenSum . "</p>";
    echo "<p>홀수의 합계: " . $oddSum . "</p>";*/

    // 2번 문제
    // $multiple = $_POST['multiple'];

    // 1부터 100까지 n의 배수 출력
    // for ($i = 1; $i <= 100; $i++) {
    //     if ($i % $multiple == 0) {
    //         echo $i . "<br>";
    //     }
    // }

    // 3번 문제
    // $number1 = $_POST['num1'];
    // $count = 0; // 배수의 개수를 저장할 변수 초기화
    // $sum = 0; // 배수의 합을 저장할 변수 초기화

    //   for ($i = 100; $i <= 999; $i++) {
    //     if ($i % $number1 === 0) {
    //         $count++; // 배수를 찾으면 개수 증가
    //         $sum += $i; // 배수를 합에 더하기
    //     }
    // }
    
    // 결과 출력
    // echo "100부터 999까지의 범위에서 {$number1}의 배수의 개수: {$count}개<br>";
    // echo "100부터 999까지의 범위에서 {$number1}의 배수의 합: {$sum}";

    // 4번 문제 (4의 배수가 아닌 것들의 갯수와 합)
    // $cnt = 0;   //갯수
    // $sum = 0;   //합
    // for($i = 100; $i <= 999; $i++){
    //     if($i%4 != 0){
    //         $cnt++;
    //         $sum += $i;
    //     }
    // }
    //echo "4의 배수가 아닌 수의 갯수 : ".$cnt.", 합 : ".$sum;

    // 6. 1부터 100까지 3과 5의 공배수를 출력하세요.
    // for($i = 1; $i <= 100; $i++){
    //     if($i%3 == 0 && $i%5 == 0){
    //         echo $i." ";
    //     }
    // }

    //8.1부터 100까지 7의 배수이거나 11의 배수를 출력하세요.
    // for($i = 1; $i <= 100; $i++){
    //     if($i%7 == 0 || $i%11 == 0){
    //         echo $i." ";
    //     }
    // }

    //10번 문제
    // if ($_SERVER["REQUEST_METHOD"] == "POST") {
    //     $count = $_POST["count"];
    //     for ($i = 0; $i < $count; $i++) {
    //         echo "Daniel<br>";
    //     }
    // }

    //11번 문제
    // $num1 = $_POST['num1'];
    // $num2 = $_POST['num2'];

    // $start = min($num1, $num2); // 작은 수
    // $end = max($num1, $num2); // 큰 수
    
    // for ($i = $start; $i <= $end; $i++) {
    //     echo $i . " ";
    // }
?>